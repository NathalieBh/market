import ProductCard from '../../../components/Products/ProductCard';
import productDetails from '../../../components/Products/productDetails';
function Cart(props) {
  // returning a page product with details and a payement button

  return;
  <ProductCard products={productDetails} />;
}

export default Cart;
